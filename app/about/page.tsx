export default function AboutPage() {
    console.log("AboutPage Rendered!"); // Debugging
    return (
        <div className="fixed inset-0 flex items-center justify-center">
            <h1 className="text-8xl text-white">About</h1>
        </div>
    );
}